meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{backend_url}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "william.jobs",
    "password": "bpm"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
