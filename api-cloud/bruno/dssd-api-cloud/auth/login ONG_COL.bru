meta {
  name: login ONG_COL
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}{{apiUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "william.jobs",
    "apiKey": "william.jobs"
  }
}

vars:post-response {
  token: res.body.token
}

settings {
  encodeUrl: true
  timeout: 0
}
