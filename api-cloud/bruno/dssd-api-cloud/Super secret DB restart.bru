meta {
  name: Super secret DB restart
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/{{apiUrl}}/system
  body: text
  auth: inherit
}

body:text {
  kaboom
}

settings {
  encodeUrl: true
  timeout: 0
}
