spring.application.name=api-cloud

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/dssd_api_cloud_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:dssd-backend}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:dssd-backend}
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

jwt.secret=${JWT_SECRET:mysupersecretkeythatishuge1234567890}
jwt.expiration=3600000

server.port=${PORT:7070}